import QtQuick

Item {
    property var darkGray: "#292929"
    property var lightGray: "#ACACAC"

    property var mainSection: "#000000"
    property var semiTransparentBlack: "#8F000000"
    
    property var pointColor: "#1DE9B6"
    property var textColor: "#FFFFFF"
    property var normalColor: "#FFFFFF"
    property var warnColor: "#E91D20"
}